uv>=0.1.34
uvloop>=0.18.0

# OpenEnv: canonical package for building/serving RL environments (create_app, env_server, types, CLI)
openenv-core[core]>=0.2.0

# Environment runtime
numpy>=1.24.0
torch>=2.0.0
transformers>=4.36.0
datasets>=2.14.0
sympy>=1.12
pydantic>=2.0.0

# Training (GRPO via TRL + vLLM inference)
trl>=0.12.0
vllm>=0.6.0

# Eval / plotting
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0
